{% macro item(icon, title, desc) %}
  <div class="info-card">
    <span>
      <i class='bx {{ icon }}' ></i>
    </span>
    <div>
      <h4>{{ title }}</h4>
      <p>{{ desc }}</p>
    </div>
  </div>
{% endmacro %}